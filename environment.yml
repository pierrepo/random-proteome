name: random-proteome
# channels priority
# see https://bioconda.github.io/
# conda-forge has highest prioriy
channels:
  - defaults
  - bioconda
  - conda-forge
dependencies:
  - python>=3.7
  - jupyterlab
  - matplotlib
  - pandas
  - biopython
